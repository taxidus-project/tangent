local core = require("src.engine.core")
local resource = require("src.engine.resource")
local stores = require("src.engine.stores")

return {
    core = core,
    resource = resource,
    stores = stores,
}
